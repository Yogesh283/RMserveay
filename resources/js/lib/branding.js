/** Public asset — `public/images/logo.png` */
export const APP_LOGO_URL = '/images/logo.png';

/** Must match `APP_NAME` in `.env` / `config('app.name')` when set */
export const APP_NAME_FALLBACK = 'RM Survey';
